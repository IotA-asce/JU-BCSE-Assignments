void polynomialRoot(int polyA[], int count);


// loop 

// ax^3 + bx^2 + cX + d
// X 
int k = 0;
for (size_t i = 0; i < count1; i++)
{
    for (size_t j = 0; j < count2; j++)
    {
        arrayTempExp[k] = array1Exp[i] + array2Exp[j];
        arrayTempCoeff[k] = array1Coeff[i] * array2Coeff[j];
        k++;

        // do here
    }
    ///* ebar compare kothai korbo*/
}
